<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, user-scalable=no">
    <title>Controller - Orgs Murder Each Other</title>
    <script src="/socket.io/socket.io.js"></script>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            touch-action: none;
        }

        body {
            font-family: system-ui, -apple-system, sans-serif;
            background: #1a1a2e;
            color: #eee;
            overflow: hidden;
            height: 100vh;
            width: 100vw;
        }

        /* Join Screen */
        .join-screen {
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            height: 100vh;
            gap: 1.5rem;
            padding: 2rem;
        }

        .join-screen.hidden {
            display: none;
        }

        .join-title {
            font-size: 1.5rem;
            font-weight: bold;
            color: #e94560;
        }

        .game-code-input {
            font-size: 2rem;
            padding: 1rem 1.5rem;
            text-align: center;
            letter-spacing: 0.3em;
            text-transform: uppercase;
            background: #16213e;
            border: 2px solid #0f3460;
            border-radius: 12px;
            color: #eee;
            width: 100%;
            max-width: 280px;
        }

        .game-code-input:focus {
            outline: none;
            border-color: #e94560;
        }

        .join-btn {
            font-size: 1.25rem;
            padding: 1rem 3rem;
            background: #e94560;
            border: none;
            border-radius: 12px;
            color: #fff;
            font-weight: bold;
            cursor: pointer;
            transition: background 0.2s;
        }

        .join-btn:active {
            background: #c73e54;
        }

        .error-msg {
            color: #ff6b6b;
            font-size: 0.875rem;
            min-height: 1.25rem;
        }

        /* Controller Screen */
        .controller-screen {
            display: none;
            height: 100vh;
            width: 100vw;
            flex-direction: row;
            align-items: center;
            justify-content: space-between;
            padding: 2rem;
        }

        .controller-screen.active {
            display: flex;
        }

        /* Joystick */
        .joystick-container {
            width: 150px;
            height: 150px;
            background: #16213e;
            border: 3px solid #0f3460;
            border-radius: 50%;
            position: relative;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .joystick-knob {
            width: 60px;
            height: 60px;
            background: #e94560;
            border-radius: 50%;
            position: absolute;
            box-shadow: 0 4px 12px rgba(233, 69, 96, 0.4);
            transition: transform 0.05s;
        }

        /* Shoot Button */
        .shoot-btn {
            width: 120px;
            height: 120px;
            background: #e94560;
            border: none;
            border-radius: 50%;
            color: #fff;
            font-size: 1rem;
            font-weight: bold;
            text-transform: uppercase;
            letter-spacing: 0.1em;
            box-shadow: 0 6px 20px rgba(233, 69, 96, 0.5);
            cursor: pointer;
            transition: transform 0.1s, box-shadow 0.1s;
        }

        .shoot-btn:active,
        .shoot-btn.pressed {
            transform: scale(0.95);
            box-shadow: 0 2px 10px rgba(233, 69, 96, 0.4);
        }
    </style>
</head>

<body>
    <!-- Join Screen -->
    <div class="join-screen" id="join-screen">
        <h1 class="join-title">Enter Game Code</h1>
        <input type="text" class="game-code-input" id="game-code-input" maxlength="6" placeholder="----"
            autocomplete="off">
        <button class="join-btn" id="join-btn">Join Game</button>
        <div class="error-msg" id="error-msg"></div>
    </div>

    <!-- Controller Screen -->
    <div class="controller-screen" id="controller-screen">
        <div class="joystick-container" id="joystick-container">
            <div class="joystick-knob" id="joystick-knob"></div>
        </div>
        <button class="shoot-btn" id="shoot-btn">MURDER</button>
    </div>
</body>
<script src="client.js"></script>

</html>